version: '2'

services:
  nginx:
    build: ./nginx
    links:
      - app
    ports:
      - 8000:80
    networks:
      - backend
  app:
    build: ./nodejs
    volumes:
      - ./web/src/public:/var/www/public
    networks:
      - backend

networks:
  backend:
    driver: bridge

volumes:
  app_data:
    driver: local